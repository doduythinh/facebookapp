import React from "react";
import Spinner from './Spinner.scss'

const spinner = () => {
    return (
        <div className="Loader">
            loading...
        </div>
    );
}
export default spinner;

